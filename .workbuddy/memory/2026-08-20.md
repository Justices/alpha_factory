# 2026-08-20

- 诊断：同批采样出两个几乎一样的 alpha（RR7WjYKa / N179x6z8，EUR/TOP1200，仅 vec_stddev vs vec_min 一处算子差异）。根因链：① 聚合算子池(avg/sum/min/max/count/stddev/range)与字段交叉生成近亲变体；② expression_sha 全等去重，字符串不同→两条都进候选池；③ sample_catalog_expressions 分层只按 batch→base_fields，同字段组内随机抽，配额≥2 时近亲同时中签。采样保证字段多样性，不保证表达式语义多样性。修复方向：字段组内加同构指纹去重（聚合算子归一化），或生成层限单一聚合算子变体。
- 已实现修复：repository.py 加 `_isomorphic_fingerprint`（vec_avg/sum/min/max/count/stddev/range → vec_AGG 归一化）+ `_pick_with_isomorphic_dedup`（组内同构折叠）+ sample_catalog_expressions 新参数 `dedup_isomorphic=True`（跨 batch/字段组全局指纹去重 + 全局补齐跳过已覆盖指纹，宁缺毋滥）。验证：真实库 14921 候选/2939 指纹（近亲冗余 80.3%），limit=1000 时残留重复 236→0。含跨 batch 场景修复（初版漏了跨 batch 同字段近亲）。
- 生成层压制（第二层防线）：fields.py 新增 `preprocess_fields_rotated`——VECTOR 字段按轮转分配单个聚合算子（seed 打乱顺序，每字段只归约 1 次，全局算子多样性保留）；ai_workflow.py 主 survey 路径 + template_library 路径改用。端到端：20 VECTOR+5 MATRIX 模拟 → 一阶任务 6380→1100（降 83%），同构冗余 5280→0，指纹数 1100 零损失（多样性全保留）。orchestrator/generation/strategies 不直接调 preprocess_field，无需改。
- vec 双重嵌套专项（老刘指出 vec_count(winsorize(ts_backfill(vec_sum(field)...))) 不该出现）：① 核实平台 Vector 算子分类 = 7 个（avg/sum/min/max/stddev/range/count），本地 vec_ops 已对齐，reduce_* 是 COMBO-only 不可用于 REGULAR；② 当前消费逻辑（template_creation_strategy + slot_context_types + vector_fields 裸字段）已防嵌套，用真实字段名重放 active 模板 0 嵌套；③ data/alpha_research.db 存量 149 条双重嵌套（origin=distilled，8/18-8/19 历史遗留）→ 已全部标记 status=pruned；④ 修复 has_vec_nesting 误判边界：裸子串 "vec_" 改正则 _VEC_OP_RE.search（字段 id 以 vec_ 前缀开头不再误判）；⑤ 修正 2 条 active 模板（chn_mined id=99/101）placeholders role scalar→vector 数据一致性。
- 按老刘指示移除 has_vec_nesting 兜底（源头 vector 槽约束已保证 vec 只作用裸字段，运行时检查冗余）：删除 families.py 的 has_vec_nesting + _VEC_OP_RE、ai_workflow._blocked 的兜底调用（保留规则库匹配）、__init__.py 导出；test_distill.py 同步删除对应测试函数、保留 vec_count(rsk60_last) 裸字段断言。全项目 0 残留，import/测试回归通过。
